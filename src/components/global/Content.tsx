"use client"

import { Box } from "@mui/material"
import { PropsWithChildren } from "react"

const Content = ({ children }: PropsWithChildren) => <Box padding="7rem 1.5rem">{children}</Box>

export default Content
