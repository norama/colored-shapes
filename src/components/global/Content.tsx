"use client"

import { Box } from "@mui/material"
import { PropsWithChildren } from "react"

const Content = ({ children }: PropsWithChildren) => <Box padding="3.5rem">{children}</Box>

export default Content
