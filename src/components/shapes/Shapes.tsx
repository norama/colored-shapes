"use client"

import { Button } from "@mui/material"

const Shapes = () => {
  return (
    <>
      <div>SHAPES cccccccc</div>
      <Button variant="contained" color="primary" onClick={() => alert("CLICK")}>
        CLICK
      </Button>
    </>
  )
}

export default Shapes
